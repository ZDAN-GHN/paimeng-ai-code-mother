# 记忆：TS Agent（Node，`paimeng-ai-code-agent/`）

> 目标架构与实施顺序权威：`docs/ts_agent/architecture.md`；wire 契约（#5 定稿）：`docs/ts_agent/contract.md`。本文件只记实施状态、决策摘要与移植指针。

## 当前状态

- **2026-09-03 用户决策：目录名复用 `paimeng-ai-code-agent/`**（旧 Python Agent 目录已整体重命名为 `paimeng-ai-code-rag/`，见 `python-rag.md`）。
- **骨架已完成（Issue #3，2026-09-03）**：Fastify 5 + TypeScript 5.9 + jose 6 + vitest 3，端口 8092；`npm test` 24/24 全绿（鉴权 401 矩阵×7、沙箱逃逸×9、SSE 顺序/格式×7）；type-check 通过。
- **run 客户端已完成（Issue #4，2026-09-04）**：`src/internal/runClient.ts`（Java 内部 API：createRun/updateRun/getRun/getLatestNonTerminalRun，Bearer 服务令牌 + JSON；409→`RunConflictError`「当前有进行中的任务」、401→`RunApiError`、网络失败→status 0）；配置新增 `JAVA_INTERNAL_BASE_URL`（默认 `http://localhost:8123/api`，含 context-path）与 `JAVA_INTERNAL_TOKEN`（与 Java 侧 `internal-api.token` 一致，.env.example 已加）；`npm test` 33/33（原 24 + run-client 9）。
- 路由：`GET /healthz`（无鉴权）、`POST /agent/workspace/validate`、`GET /agent/smoke/sse`（均 JWT 保护，钩子封装在插件作用域不外溢）。
- JWT 约定：HS256 共享密钥（`JWT_SECRET`），`algorithms` 白名单防混淆，`requiredClaims: ['exp','sub']`，离线验签不回查 Java；Java 侧签发在 #12。
- `WORKSPACE_ROOT` 默认按服务目录解析 `../tmp/code_output`（对齐 Java `user.dir/tmp/code_output`），沙箱校验含 realpath 符号链接消解（移植 manager.py）。
- 测试基建约定（后续票据沿用）：真实服务实例（`buildApp(overrides)`）+ `fastify.inject()` 注入式请求 + `jose SignJWT` 自签（`test/helpers.ts`）。
- **运行时目录按宿主分流（2026-09-07 起）**：原生 Linux/Windows 用服务目录默认 `node_modules`+`dist/` 与标准 `npm run`，无需指定环境目录；`wsl-rt-env/ts-agent/` 仅限 WSL 宿主。`scripts/run.mjs` 按 `/proc/version` 是否含 `microsoft` 判定 WSL（原生 Linux 上 type-check/build 已实测通过，产物落服务目录 `dist/`）。
- 运行时环境布局（2026-09-04 起，**完全无软链**）：依赖与产物都在 `wsl-rt-env/ts-agent/`（`node_modules` + esbuild 产物 `dist/app.bundle.mjs`），服务目录内零 node_modules、零符号链接。WSL 通过 `bash scripts/run-wsl.sh <dev|start|build|test|type-check>` 进入 npm 调度器，脚本先校验 Linux/WSL；Windows/IDE 继续使用原有 npm 运行配置。构建模式 **esbuild 打包**（原 tsc 产物方案已废弃，`tsconfig.build.json` 已删）：ESM bundle + banner 注入 `createRequire`（fastify 内部有 CJS require），产物自包含、运行期不需要 node_modules；`src/config.ts` 以 `AGENT_ROOT` 环境变量定位服务目录（`.env`/`WORKSPACE_ROOT` 基准），由调度器注入。测试 `vitest.config.mjs` 用 `resolve.alias` 指向 wsl-rt-env 的包；类型检查 `tsconfig.json` 用 `paths` 映射各包 d.ts 文件（NodeNext 下 paths 目录映射无效，必须是文件；候选本地优先）。安装命令 `bash scripts/install-wsl-node-modules.sh`，运行入口 `bash scripts/run-wsl.sh`（默认 `dev`）。dev 重启链路由 esbuild watch `onEnd` 驱动（DrvFs 上 `node --watch` 收不到文件事件，实测确认）。
- **最小生成流已完成（Issue #5，2026-09-04 重做定稿）**：`POST /agent/stream` = **XState v5 线性工作流（`src/machine.ts`，interview→coding→review→done/failed）+ Vercel AI SDK provider 抽象假 LLM（`src/llm.ts`，`LanguageModelV2` + `customProvider` 注册 `scripted` 模型，零在线调用）+ `src/workflow.ts` 驱动**（`streamText` + `writeFile` 工具 + `stopWhen: isStepCount(2)` 工具循环，`fullStream` part → SSE 事件；AI SDK v7 已无 `maxSteps`）。成功路径推进 `interview → coding → review → done`，输出 milestone / ai_thinking / ai_response / tool_request → tool_executed / done；`script: error` 终止于 error（后无业务事件）且 run→failed。模型抛错被 AI SDK 吸收为流内 `error` part（循环正常结束），须在 switch 里显式抛出才能走 failed。工作区写入经沙箱校验，Java token 存在时经 #4 `RunClient` 更新 run；无 token 离线可跑。契约定稿 `docs/ts_agent/contract.md`，测试按语义断言；WSL `bash scripts/run-wsl.sh type-check` 通过、`bash scripts/run-wsl.sh test` **37/37**。运行时：`ai`/`xstate`/`@ai-sdk/provider` 落 wsl-rt-env（tsconfig paths + vitest alias 补映射）；**vitest 测试默认 `--configLoader runner`**（`$HOME/node_modules/.vite-temp` 被 Windows ACL 锁死 EACCES，runner 不打配置包绕开）。
- **Agent→Java 回调打通已完成（Issue #6，2026-09-04）**：`runClient` 新增 `completeRun(runId, AgentCompleteRequest)`（POST `/internal/agent/runs/{runId}/complete`，Bearer + runId 幂等）；`workflow.ts` 新增 `notifyComplete()`——终态时写本次对话历史（messages user/ai）+ Java 触发构建。**踩坑（实测）**：回调必须放在 `yield {type:'done'|'error'}` **之前**——路由收到终态事件即 `break`，async generator 不再被 resume，yield 后的代码永远不执行。`appId`/`userId` 走字符串传输（Java Long ToStringSerializer，防 JS 精度丢失；实测数字字面量 453132478241230849 丢精度变 +1）。route 层把 JWT `sub` 兜底的 userId 注入 workflow 请求（`input.userId = userId`）。回调失败不阻断主流程（console.error 告警，Java 幂等兜底）。测试：`stream.test.ts` 断言完成回调（success/failed + messages + workspacePath），**37/37**。
- **需求工程完成（Issue #7，2026-09-04）**：① `src/interview.ts` 五维访谈（受众/风格/页面清单/数据需求/交互，每维 2-4 选项选择题，`MAX_INTERVIEW_ROUNDS=2`，各维均作答即收束跳过剩余轮次；状态持久化于 `run.context.interview` 跨请求存活）；② `src/wireframe.ts` 快速档线框（单文件 HTML：灰块 + 占位图 + 页内锚点可点击跳转 + 站点地图，`PAGE_LIMIT=5`，存 `{workspace}/wireframe/wireframe.html`）；③ `src/context.ts` run.context 类型化解析（interview + wireframe）；④ 路由 `POST /agent/interview` / `/agent/wireframe` / `/agent/wireframe/confirm`（`/agent/wireframe` 生成前调 Java 配额端点，超限 → 429 明确报错；confirm 幂等）；⑤ **codegen 闸门**：`/agent/stream` 先经 `runClient.getRun` 校验，非 `wireframe_confirmed` 阶段输出唯一 `error` 事件「未确认线框，无法进入代码生成（{阶段}）：请先完成访谈并确认线框」——不再 createRun（run 由访谈建立）。测试：新增 `test/requirements.test.ts`（8 例，内存版 Java 内部 API 模拟跨请求持久化）+ `test/stream.test.ts` 闸门 3 例；**48/48 全绿**。**测试基建坑**：`.env` 的 `JAVA_INTERNAL_TOKEN` 会被 `loadDotEnv` 读进 config → `buildTestApp` 默认离线必须显式 `javaInternalToken: ''`（已加），否则测试误连真实 Java。
- **#7 代码审查整改（2026-09-04，/code-review 后）**：`@Override` 方法 `acquireWireframeDailyQuota` 删掉重复 Javadoc（rationale 挪入方法体行内注释，接口文档保留）；**`wireframe_pending` 下重新访谈 → 回到 interview 并失效旧线框**（本地 `context` 同步 delete 防 persist 重写）；**`/agent/stream` 未配置内部 API 时拒绝放行**（error 事件，不再离线静默绕过闸门，与需求工程端点 503 口径一致）。TS **50/50**、Java 35/35。
- **生成核心移植完成（Issue #8，2026-09-04）**：① Guardrail（`src/guardrails.ts`，从 Python/Java 逐条移植；workflow 在 interview 阶段 coding 前校验，拒绝 → failed + 明确报错，machine interview 补 FAIL 转移）；② 代码块解析（`src/codegen/parsing.ts`，正则逐字对齐 Python，writeFile 写盘前经解析文件集落盘）；③ 文件六工具（`src/tools/fileTools.ts`：writeFile/readFile/modifyFile/deleteFile/readDir/exit + IMPORTANT_FILES 保护 + FilePathError 越界）；④ 图片四工具 + 配额（`src/tools/imageTools.ts`：Pexels/Undraw/DashScope/mmdc，HTTP/renderer 可注入，**图片配额 4 张/run**，用尽返回 `IMAGE_QUOTA_EXCEEDED_MESSAGE`）；⑤ 提示词 7 份复制进 `src/prompts/`（`src/prompts.ts` 经 AGENT_ROOT 加载；codegen 三份注入「导览组件强制产出」要求）；⑥ workflow 全套 10 工具注册（`src/tools/index.ts` buildTools）+ codegen 提示词作 system；⑦ fake LLM 扩展（`buildPageContent` 产物内嵌 onboarding-tour 应用内组件；新增 `images` 剧本一轮并行图片搜索触发配额）。**验收证据**：`bash scripts/run-wsl.sh type-check` 通过；`bash scripts/run-wsl.sh test` → **85/85 passed**（原 50 + guardrails 6 + parsing 6 + fileTools 12 + imageTools 7 + stream #8 4）；验收逐条勾选：工具成对事件+落盘（成功/images 剧本按 id 配对断言）、图片配额被拒（取满 4 张后第 2 次拒绝）、Guardrail 6 用例、解析 6 用例语义等价、产物含导览组件（`onboarding-tour`）。配置新增 `PEXELS_API_KEY`/`DASHSCOPE_API_KEY`/`IMAGE_MODEL`（.env.example 已加）。**踩坑**：AI SDK 并行执行工具时 tool-result 顺序与 tool-call 可不同（断言须按 id 配对，不能按数组顺序）；JS 正则用 `exec`（不是 Python 的 `search`）。
- **#12 前端通道切换已完成（2026-09-04）**：Java 签发侧（`GET /app/agent/token`，详见 java-backend.md）+ 前端 fetch-SSE 直连（详见 vue-frontend.md）。**Agent 侧零代码改动**（JWT 验签 #3 已就位，hutool 签发与 jose 验签互操作经 e2e 实测）；`JWT_SECRET` 与 Java `agent.jwt.secret` 已同步同一随机值。**e2e 踩坑**：被 #7 闸门拒绝的 run 停在 interview 非终态，会挡同 app 后续 createRun（409「当前有进行中的任务」）——测试残留需经内部 API `PATCH /internal/runs/{runId}` phase=failed 清理；另外 Java 响应 Long 全字符串序列化（expiresAt 也是字符串）。
- **#9 质检门禁 + 护栏 + 三档 + token 计量已完成（2026-09-05）**：① **三工位循环**（machine.ts coding↔review 有界重试 `MAX_QUALITY_RETRIES=2`，review.RETRY guard 由图钉死，重试次数经 context.qualityAttempts 计数，首次 + 最多 2 次重试 = 共 3 次尝试）；② **三重门禁 reviewer**（`src/review/`：QualityScoreGate 结构化质检分（generateText + `scripted-quality` 模型 + code-quality-check 提示词，isValid/errors/suggestions，解析失败视为未通过）+ BuildGate（html 静态校验 index.html 含 `<html>` 根 / multi_file|vue_project 明确提示 L1 构建管线未接入）+ VisualDiffGate（**基准 = 已确认线框**：路由从 `run.context.wireframe.relativeUrl` 解析绝对路径经沙箱校验传入，结构启发式对比 page-N 区段覆盖；Playwright 渲染级 diff 属远期））；③ **护栏前三层**（`src/intensity.ts` INTENSITY_TIERS 三档 fast/standard/deep：maxTurns/maxOutputTokens/maxToolCalls/maxImages 随档位放大，标准档 max_tool_calls=50、max_images=4 对齐先例；streamText 传 maxOutputTokens + `stopWhen:[isStepCount(maxTurns), stopWhenToolCalls(maxToolCalls)]`；**超限 = 优雅收尾**：finishReason=tool-calls 判定截断 → generateText 注入收尾指令输出完整交代 → 状态机仍按拓扑推进（coding→review→done）不卡 coding，**绝不硬杀**；`src/workflow/history.ts` windowHistory 最近 10 轮全文 + 更早摘要并入 system）；④ **token 计量**：每轮 streamText/generateText usage 累计 + 质检门禁 usage 经 GateResult.usage 透传，终态前 `runClient.updateRun({ tokenUsage })` 落库（done/failed 都落）；⑤ **三档路由**：`provider.languageModel(resolveModelId(tier))`，config 可经 MODEL_FAST/MODEL_STANDARD/MODEL_DEEP 覆盖模型 id（.env.example 已加注释占位）；请求体新增 `intensity`/`history`/`codeGenType`（默认 html）；⑥ **假 LLM 扩展**：三档模型（scripted-fast/standard/deep）+ `scripted-quality` 质检模型 + records 记录调用（modelId/maxOutputTokens/hasToolResult 供断言）+ 新剧本 limit（工具调用无休止）/quality-fail-then-pass（第 1 次质检失败）/quality-fail-always。**验收证据**：type-check 通过；`bash scripts/run-wsl.sh test` → **128/128 passed**（原 86 + intensity 5 + history 6 + review 18 + quality-gate 13）；`npm run build` 产物 2.3MB；git diff --check 通过。**踩坑**：AI SDK 的 `PASS` 事件在 coding 状态无转移定义（send 被忽略）→ 超限收尾必须 PROCEED 进 review 再 PASS，否则 phase 卡 coding 里程碑缺失；vitest 默认 5s 超时对成功剧本（3-5s）全量并行偏紧，vitest.config 提到 20s（真死循环仍会暴露）。
- **#10 积分冻结 + 对话中断已完成（2026-09-05）**：① **冻结**——`runClient.freezeCredit(runId, {intensity})`（POST `/internal/agent/runs/{runId}/credit/freeze`，402→RunApiError.status=402）；`/agent/stream` 闸门通过后、workflow 前调用（**冻结时点 = 进入 codegen**，架构 §7；余额不足 402 → 唯一 error 事件拒绝，零 token 消耗；幂等 Java 侧不重复扣）。② **对话中断（架构 §3.5 中止 (a)）**——route `reply.raw.on('close')` + `!writableEnded` 判断客户端断开 → `abortController.abort()`；workflow 传 `abortSignal` 给 streamText/generateText，error part 分支 `isAbortError`（name==='AbortError'）→ 抛 `GenerationAborted` 哨兵；**关键决策点 `throwIfAborted`**（每轮循环顶 + done 前）覆盖「工具执行后调用间隙」的 abort（仅靠 AI SDK 流内感知不可靠，tool-executed 后 abort 会漏到 done——e2e/单测实测）；顶层 catch 区分 `GenerationAborted`/`AbortError` → `abortRun`（保留已写文件 + updateRun phase=aborted + notifyComplete('aborted', filesWritten) + error 事件「生成已中断」）。③ **文件计数**——`FileTools.filesWritten`（writeFile 成功 +1；读/改/删不计数）。④ **notifyComplete 三态**——success/failed/aborted，aborted 带 `filesWritten`；failed 的 ai 消息为空（Java 侧跳过空消息，见 java-backend.md 坑）。**测试**：137/137（原 128 + `test/abort.test.ts` 3（落盘后中断 filesWritten=1 / 首文件前中断 =0 / 哨兵）+ stream 冻结 2（402 拒绝/成功放行）+ runClient 3（freeze 请求形状/402/aborted filesWritten）+ fileTools 1（计数））。**e2e 踩坑**：默认 reviewGates 真实执行视觉 diff（基准=已确认线框），假 LLM 产物仅 page-0 单页区段 → 3 页线框必失败重试耗尽 → e2e 成功剧本须选单页线框（pageCount=1）。
- **#11 对账审计结论修正（2026-09-07）**：8a16cf7 审计曾基于本地 clone（远程 main 停在 #6、未 fetch 到新提交）判定 #7-#10/#12「幻影关闭」；经用户指正实现一直在远程仓库，已合流 main——**结论作废**。`contract-parity.md` 84 例清点仍有效，覆盖/缺口判定待 #11 基于真实实现重跑修正。
- **#11 契约对账完成（2026-09-07，T21 判据在档）**：基于真实实现重跑 84 例逐文件对账，报告终稿 `docs/ts_agent/contract-parity.md`（72 覆盖 / 3 语义差异 P3 定夺 / 9 有意演进·退役，判据均引架构或票据记录）。**本票补齐缺口 5 项**：① golden e2e 全链 ×2（`test/golden-e2e.test.ts` + `test/fixtures/golden_{html,multi_file}.json`，假 LLM 新增 `multi-file` 剧本一轮并行写三文件，routes SCRIPT_WHITELIST 放行）；② 质检拼接补强（dist 跳过断言 + 目录缺失返回空，`review.test.ts`）；③ 十工具名驼峰绑定契约（`test/tools-contract.test.ts`，Java ToolManager 依赖）；④ 护轨拒绝 → failed 回调断言（`stream.test.ts` 既有用例追加）；⑤ 完成回调失败不阻断主流程（`stream.test.ts` fakeRunClient 注入 /complete 500）。**验收**：`npm test` **144/144**（19 文件）+ type-check 通过。**P3 移交修正项 5 条**（报告 §五）：codeGenType 白名单外回退 vs 旧 422、contract.md message 必填 vs 路由只查 runId/appId/userId、质检解析失败兜底方向（有意反转待写入契约）、`src/codegen/parsing.ts` 与多份提示词死代码（主链路仅消费 codegen-html）、单文件写入原子性评估。
- **#11 code-review 双轴整改（2026-09-08，/code-review 后）**：Standards 0 硬违规 + 4 判断性提示；Spec 4 条 AC 全支撑 + 3 发现。整改 4 项：① `frames`/`fakeRunClient`/`RunCall` 从 stream.test 与 golden-e2e 双份定义提取至 `test/helpers.ts`（golden 版本已分叉，统一为带 `gatePhase`/`completeFails` 参数的版本）；② `tools-contract.test.ts` 硬编码 `/tmp` 改用 `makeWorkspaceRoot()`；③ contract-parity.md §四证据订正（#10 交付 70/70 → 整改后 77/77 为最新在档）；④ MEMORY.md 补末尾换行。**未整改 2 项留档**：新增剧本 4 处编辑的 Shotgun Surgery（可聚映射表）、`GoldenFixture.codeGenType` 含 `'vue_project'` 无夹具（暂收窄无必要）。**mock 层测试债记 #13**：`llm/index.ts` buildPageContent 约 100 行 MSW 风格拦截层系随 2519ced 提前落地（#13 L1 预览态，无测试覆盖），已在 #13 票 + 报告 §一范围注记留账。TS **144/144** + type-check 通过。
- 实施票据链：~~#3 骨架~~ → ~~#4 run 生命周期~~ → ~~#5 最小生成流 + 契约定稿~~ → ~~#6 回调打通~~ → ~~#7 需求工程（访谈/线框/确认闸门）~~ → ~~#8 生成核心移植（工具四件+解析+Guardrail）~~ → ~~#12 前端通道切换~~ → ~~#9 质检门禁+护栏+三档+token 计量~~ → ~~#10 积分（冻结/结算/退款+对话中断）~~ → ~~#11 对账（T21 契约对等判据通过）~~ → #13 前端功能补齐 → #14 灰度+T21。

## 移植参考（Python Agent 实测资产，代码现位于 `paimeng-ai-code-rag/`）

- 提示词 7 份：`paimeng-ai-code-rag/app/prompts/`（源 `src/main/resources/prompt/*.txt`）
- 解析正则：`paimeng-ai-code-rag/app/services/codegen/parsing.py`；guardrail：`paimeng-ai-code-rag/app/core/guardrails.py`
- 工具沙箱：`paimeng-ai-code-rag/app/workspace/manager.py`（`validate_workspace_path` + `atomic_write_files`）；图片四工具：`paimeng-ai-code-rag/app/services/images.py`
- 契约测试模式：`paimeng-ai-code-rag/tests/test_contract.py`（TS 侧按语义比对移植）

## 移植要点（Python 实测契约教训，TS 重写必须保持）

- vue 工具名/参数键必须驼峰（`writeFile/readFile/modifyFile/deleteFile/readDir/exit`、`relativeFilePath/oldContent/newContent`…），否则 Java `ToolManager` 与浏览器展示取 null。
- exit 工具的模型调用不确定：模型直接给最终答案时须补发 exit 事件（`exit-{uuid}`），保证事件序列与基线一致。
- 工作区原子写入：临时 stage 目录必须建在目标**父目录**（sibling），建在目标内部则 rename 时 stage 随之移动导致路径失效。
- Java `bodyToFlux(ServerSentEvent)` 解码 SSE 会产生 `data=null` 空事件：Java 客户端侧需 `filter(event -> event.data() != null)`（#6 泛化回调客户端时注意）。
- **Java 内部 API 的 Long 序列化为字符串**：`JsonConfig` 的 ToStringSerializer 会把 Long（appId/userId）序列化为字符串（防 JS 精度丢失，snowflake 18 位超安全整数）——TS run 客户端 `appId/userId` 类型必须兼容 `string | number`（#4 实测：响应 `"appId":"903"`）。

## 2026-09-05 #10 code-review 整改（TS 侧）

- **`FileTools.filesWritten` 语义修正（AC2「落盘文件数」）**：字段改 `private readonly writtenFiles = new Set<string>()` + `get filesWritten()` 返回 size——同文件重复写去重、**`modifyFile` 落盘同样计数**（续跑场景首笔磁盘写可能经 modifyFile，此前误判 filesWritten=0 → 全额退款）、`deleteFile` 成功后从 Set 移除。工作流 `catch` 里 `files?.filesWritten ?? 0` 语义随之变为「当前已落盘不同文件数」。
- **reviewer 工位 LLM 取消全覆盖（Spec：review 的 generateText 不受 abort 约束）**：abortSignal 贯穿链——`RunReviewOptions.abortSignal` → `runReviewCycle` → `gatesFromSet(set, signal)` → `QualityScoreGate(scorer, signal)` → `QualityScorer.score(code, signal)`（接口第二参可选，测试替身 `{ score(code) }` 不受破坏）→ `LlmQualityScorer` 的 generateText `...(signal ? { abortSignal: signal } : {})`。
- **abort 条件展开提取**：workflow 定义 `withAbort<T>(opts, signal)`（signal 存在才展开 `abortSignal`，AI SDK 不接受 undefined），streamText/generateText 两处重复消除。

## 下一步 / 指针

- #9：已完成 质检门禁 + 输出护栏 + 三档强度 + token 计量（详见「当前状态」与 progress.md）。
- npm 坑：仓库根目录跑 npm 会读到 `/mnt/c/Users/LXH/.npmrc` 报 "config prefix cannot be changed"——命令必须在 `paimeng-ai-code-agent/` 目录内执行（有 package.json 后 project-config 解析止于该目录）。
- 进度日志：`docs/ts_agent/progress.md`（每完成一票追加一行，含命令证据）。
