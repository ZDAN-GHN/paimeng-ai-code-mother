# 错误处理

错误分为输入校验、未认证/无权限、业务拒绝、依赖失败、超时/中断和未知异常。对用户返回稳定、可行动的消息；对日志保留原因和关联标识，不把堆栈、密钥或内部地址返回浏览器。

前端必须处理 Axios 错误、非预期响应、空数据、重复提交、SSE error/done 和连接断开。Agent 流必须发送协议规定的终态事件，连接关闭不能被当作成功完成。

跨服务调用设置明确超时并快速失败；不要在错误处理里偷偷切换旧链路、重试无上限或吞掉失败。修复错误时先保留最小复现和首个根因异常。
