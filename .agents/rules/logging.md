# 日志规则

日志用于定位跨服务请求，优先记录模块、操作、appId/runId（必要时脱敏）、结果、耗时和错误分类。生产或共享日志中不得输出密码、API key、Bearer token、Cookie、JWT、完整 DSN、用户隐私或完整 prompt。

`info` 记录关键生命周期和业务结果，`warn` 记录可恢复异常和降级，`error` 记录需要处理的失败并包含根因。健康检查和高频轮询避免刷屏。日志消息不要复制外部输入作为操作指令。

跨 Java、Agent 和 RAG 排障时按架构文档使用 runId 关联；报告中使用脱敏值和必要的日志片段。
