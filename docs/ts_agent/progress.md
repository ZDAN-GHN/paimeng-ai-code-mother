# TS Agent 实施进度日志

> 按 `.agents/memories/README.md` 更新约定：每完成一个任务追加一行，含日期与命令证据。Python Agent 时代历史见 `docs/py_agent/progress.md`。

## 2026-09-08

- **#14 灰度切换 + T21 执行完成（Issue #14，P3 收官）**：① **ts-agent.enabled 灰度开关**——`AgentProperties` 前缀 `agent.*`→`ts-agent.*`（`enabled` 默认 **true** 门禁 `GET /app/agent/token`，关闭→新 `ErrorCode.AGENT_DISABLED(40410)` 明确报错「代码生成新链路未开启」；旧 Java AI 删除后回退手段=git 回滚，开关不再承担回退职责）；`AgentJwtProperties`→`ts-agent.jwt`；`application.yml` 的 `python-agent.*`/`agent.*` 两段合并更名 `ts-agent.*`（环境变量 `TS_AGENT_*`），`AgentLegacyAliasPostProcessor`+`spring.factories` 删除（别名键全属中转链）；`GenerationRunServiceImpl` 线框限频改 `AgentProperties` 单源注入（消除 @Value 双通道默认值双写）。② **T21 根因清除（115 文件，+109/−6634）**——按 `t21_delete_plan.md` 清单 + 用户 2026-09-08「根因清除」决策：删旧 Java AI（`ai/codegen` 除 route、`langgraph4j` 全部 24 类、`ai/guardrail`、`core/handler|parser|saver`、`AiCodeGeneratorFacade`、`ClazzScanner`、`PaimengAiService`/`AiGenerateModeEnum`/`ai/model` 全部）、Python 中转链五件（`AgentClient`/`AgentSseAdapter`/`AgentRequest`/`AgentCallbackRequest`/`RunIdSinkRegistry`，其端点 `/v1/agent/stream` 随 Python Agent 退役、#12 后前端无调用方）+ 端点 `/chat/gen/code` 与 `/chat/gen/code/callback` + 级联死代码（`core/handler`、`ai/tools`、`Chat/Reasoning` 流式模型 Config、`RedisChatMemoryStoreConfig`、别名处理器）+ 旧测试 16 文件；**保留项完好**：createApp 路由链（route/* + RoutingAiModelConfig + SpringContextUtil + langchain4j）、BuilderExecutor、内部 API（含 `AgentCompleteRequest`/`AgentCompleteStatusEnum`）、JWT 签发三件、积分协议。pom：删 `langgraph4j-core`/`langchain4j-reactor`/`langchain4j-community-redis-starter`/`webflux`，**补显式 `spring-boot-starter-data-redis`**（RedisConnectionFactory 原靠 community-redis 传递引入的隐性依赖，删除后 contextLoads 暴露）；新增 `application-local.yml.example` 模板（补 `langchain4j.open-ai.routing-chat-model` 段，t21 L84 核对项）。③ **验收证据**——AC1 两段式（用户确认语义）：**删除前**开→直连全旅程 e2e（token 200 → interview 收束 → wireframe_pending → confirm → stream 七类事件 done，run phase=done + finishedTime 落库，冻结 100=html×standard → SETTLED、余额 100000→99900）、关→token 40410 + 旧链路 SSE 信封实证（`data:{"d":...}`+`event:done`；完整生成既有权威证据为 P0 全链路，当日本地上游 SiliconFlow 延迟波动 60s 超时未复现完整流，限制入档）；**删除后**关→40410、旧端点 404、业务端点 200、默认开→token 200（灰度矩阵四象限全实证）。**Java 回归 91/92**（旧 AI 不稳定测试随删除出清；唯一失败 `AiCodeGenTypeRoutingServiceTest` 为真实 LLM 调用，上游延迟抖动，单跑 182.5s 通过、非回归）；TS Agent 144/144 + type-check 绿；契约对等报告在档（`contract-parity.md`，T21「契约对等」半边判据通过）。**双轴 code-review 整改**：AgentProperties 五个中转字段漏删（Javadoc 与代码矛盾）、wireframe-daily-limit 双通道、AppController 序号 2.5、`AgentIntensityEnum`/`AgentJwtService` 旧键名引用——全部修复后复验。④ 文档同步：`t21_delete_plan.md` 顶部执行注记（范围差异逐条）、`architecture.md` §10（退役完毕 + 开关语义）、`CONTEXT.md`（AI 层/领域概念/技术决策）、`AGENTS.md`（架构现状 + ts-agent.enabled 硬约定）、记忆域四件 + MEMORY.md。Python Agent **目录删除**不在本票（门槛=P4 RAG 骨架复用），维持「目录待删」。

- **#13 前端功能补齐完成（Issue #13）**：① **需求旅程状态机**（`AppChatPage.vue` journeyPhase：idle→interviewing→wireframe_pending→wireframe_confirmed→生成→idle）——首次发送即创建 runId 并调 `POST /agent/interview`，五维选择题以 `InterviewQuestionsCard` 卡片进对话流作答；五维全答即收束（e2e 实证跳过第 2 轮）；收束自动 `requestWireframe`，`WireframeReviewCard` 提供「确认 / 重新生成线框 / 重新访谈」三动作（闸门的用户侧）；确认后进入 codegen。**生成必须复用旅程 runId**（线框闸门与积分冻结都校验该 run 的 phase——原 createRunId 直连必被闸门拒）。② **三档强度选择器**（`IntensitySelector.vue`，a-segmented 常驻输入框旁）：选择随 `/agent/stream` 请求体 `intensity` 下发，预估积分 = 基础价 100 × 类型系数(html1/multi2/vue3) × 档位系数(fast1/standard1/deep2)，与 Java `AgentProperties.Credit`/`AgentIntensityEnum` 默认值对齐。③ **中止按钮**：生成中发送键切换为「停止」→ `streamAbortController.abort()` 断开连接 → Agent 感知取消 LLM → aborted 终态 → Java 按里程碑折算退款，catch 区分 AbortError 渲染「已中断」并刷新余额。④ **积分显示**：header 金色 tag（`GET /credit/balance`），挂载/终态/error 事件后刷新；新增手写 `api/creditController.ts`（CreditController 未纳入 openapi2ts）。⑤ **L1 预览态 mock（fixture 先行）**：`src/llm/index.ts` buildPageContent 产物内置 MSW 风格拦截层（fetch/XHR monkey-patch + fixture 规则表 GET/POST `/api/items`，POST 回显请求体），页面自带列表加载 + 表单提交演示——预览 iframe 内交互全可演示（架构 §L1）；`agentSse.ts` 扩展需求工程三端点（`requestInterview`/`requestWireframe`/`confirmWireframe`，统一 JWT + `AgentStreamHttpError` 状态码透传）+ stream 参数加 intensity/history。**HTTP 层 e2e（curl 全链路，原生 Linux + docker compose MySQL/Redis + Java 8123 + TS Agent 8092，真实登录态 JWT）**：访谈出题/收束 → 线框落盘（静态资源 200，URL 拼法与前端 `buildWireframeUrl` 一致）→ 确认 → stream(intensity=deep) 完整七类事件 done → **冻结 600 = 100×3(vue_project)×2(deep)、台账 SETTLED、余额 10000→9400** → 中止剧本（30ms 内断开）→ **aborted + REFUNDED 全额退** → 闸门拒绝（未确认线框明确报错）→ 402 余额不足（「当前余额 50，本次生成需 300 积分」，无台账记录）→ 产物 index.html 含 mock 拦截层（fetch/XHR patch、FIXTURES、列表/表单元素）。0.4s 断开时假 LLM 已瞬时 done（SETTLED）——**假 LLM 毫秒级产出使 HTTP 层中止窗口极窄**，30ms 可稳定复现，中止语义另有 #10 单测 3 例在档。浏览器 UI 层人工走查留待用户（同 #12 口径）。验证：前端 `npm run type-check`/`lint` 绿；TS Agent `type-check` + `test` **144/144**（含并行会话 #11 golden e2e，双方改动合流兼容）；`npm run build` 2.4MB。**联调环境补齐（原生 Linux 首次起全栈）**：`application-local.yml` 补 `agent.jwt.secret`/`internal-api.token`/COS 五键占位（CosClientConfig 条件装配缺失导致 CosManager 无 bean 启动失败）/langchain4j 路由模型（硅基流动 OpenAI 兼容端点，Qwen2.5-7B 响应 40-60s 偏慢）；**本地库 DDL 增量补齐**（09-07 建库用旧 DDL：ALTER user ADD credits + CREATE credit_ledger）；TS Agent `.env` 新建（JWT_SECRET 与 Java 同值）。文档同步：`docs/ts_agent/progress.md`、`MEMORY.md`、`.agents/memories/vue-frontend.md`。

## 2026-09-07

- **#11 对账审计结论修正（推翻 8a16cf7）**：审计提交 8a16cf7 基于 2026-09-06 本地 clone（远程 main 停在 #6、fetch 未获新提交）判定 #7/#8/#9/#10/#12「五票幻影关闭」；经用户指正实现提交一直在远程仓库，本地合流后确认 #7-#10/#12 全部实现与测试在档——**「幻影关闭」结论作废**。`docs/ts_agent/contract-parity.md` 的 84 例逐文件清点表保留有效，「覆盖/缺口」判定基于 TS 37/37 旧基线，#11 对账须重跑修订；T21 判据结论同步作废待重验。

## 2026-09-05

- **#10 code-review 双轴整改闭环（2026-09-05，#10 交付后补跑审查并按发现整改）**：Standards 轴 5 项 + Spec 轴 9 项发现全部处置——① **魔法值枚举化**：新建 `AgentIntensityEnum`（fast/standard/deep，`calcFrozenAmount` 强度档 switch 改枚举，空/非法兜底 standard）；台账 reason 魔法字符串（complete/failed/interrupted）收敛进 `AgentCompleteStatusEnum.reason` 字段（单一来源，`status.getReason()` 取用）。② **并发读改写 → DB 原子 SQL（AC4 并发双重退款修复）**：`UserMapper` 增 `@Update` 原子扣减/增加（`deductCredits` WHERE credits>=amount 不超扣 / `addCredits`），`UserService.deductCredits/addCredits` 透传，`doFreeze`/`recharge`/`refundBalance` 弃 updateById 读改写（同用户并发冻结不再互相覆盖丢更新）；`CreditLedgerMapper.transitionIfFrozen` 条件更新（`UPDATE ... WHERE id=? AND status='FROZEN'`），settle/refund 仅 `affected=1` 的线程执行加钱/结算——并发重复回调只一笔生效，**杜绝双重退款**（e2e 并发双 failed 回调实测只退一次）。③ **AC5 迟到回调防护**：completeRun 前置两道校验——台账已非 FROZEN（已结算/退款）→ 幂等丢弃（不依赖内存集合，进程重启丢 completedRunIds 也成立）；run 已终态但与本回调 status 不符 → 拒绝处理（迟到错序不得改写 run 状态，`terminalPhaseOf` 映射 SUCCESS/FAILED/ABORTED→done/failed/aborted）；completeRun 三分支改显式 `else if ABORTED` + 兜底抛（未知 status 不再静默走 aborted 折算）。④ **去重/改名**：`requireFrozen`→`getFrozenOrNull`（名实相符）、`balanceOf(User)` 提取（4 处空值守卫收敛）、`toFreezeVO` 移入 `CreditService.buildFreezeVO`（GenerationRunServiceImpl 私有重复构造删除）、`recharge` 返回 `boolean`→`void`（恒 true 无信息量）、接口 `@param` 对齐。⑤ **TS 侧**：abort 条件展开提取 `withAbort()`（streamText/generateText 两处重复）；**reviewer 工位 LLM 取消全覆盖**（Spec 发现：review 质检 generateText 不受 abort 约束）——`RunReviewOptions.abortSignal` → `gatesFromSet(set, signal)` → `QualityScoreGate(scorer, signal)` → `QualityScorer.score(code, signal)` → `LlmQualityScorer` generateText 传 abortSignal；`FileTools.filesWritten` 字段改 `Set` 去重 + getter（AC2 语义修正：同文件重复写不累加、**modifyFile 落盘同样计数**——续跑场景首笔磁盘写经 modifyFile 不再误判 filesWritten=0 全额退款、deleteFile 后移除）。验收证据：Java 相关测试 **77/77**（CreditServiceImpl 22 + GenerationRunServiceImpl 33 + 两个 Controller 22；新增并发条件更新/迟到错序/台账终态/非法强度/充值用户缺失等 9 例）；TS **type-check** + **139/139**（fileTools 计数语义改 1 例 + 新增续跑 modifyFile 计数 1 例 + review abort 透传 1 例）；全量 mvn 123 测 = **1 failure + 10 errors 均为基线既有环境性失败**（langgraph4j/LLM key、图片 API、截图），无回归；build 2.4MB。**WSL 实库 e2e 验证 @Update SQL（单元测试不解析 SQL，必须实库）**：踩坑——`transitionIfFrozen` 首版用下划线列名（settle_amount）而 `credit_ledger` 表列是驼峰（settleAmount，实体 `@Column` 名）→ `Unknown column 'settle_amount'`，改驼峰后全通；实库验证 freeze 原子扣减（1000→900）+ success→SETTLED(done, 结算不退) + failed→REFUNDED(failed, 空 ai 消息不阻断, 退款加回) + aborted filesWritten=1→PARTIAL_REFUNDED(settle50/refund50, aborted, 历史 `[用户中断]` 标记) + **幂等**（重复 completeRun 台账 1 行不变）+ **AC5 迟到错序**（done run 收 aborted 无副作用）+ **AC4 并发双 failed 只退一次**（credits 只加回一次，非双倍）。commit：`<#10 整改 commit>`。

- **#10 积分协议 + 对话中断完成（Issue #10，2026-09-05）**：① **Java 积分台账**——`credit_ledger` 表（uk_runId 幂等，FROZEN/SETTLED/PARTIAL_REFUNDED/REFUNDED，同库同事务）+ `user.credits` 余额 + 管理员充值 `POST /credit/recharge`（@AuthCheck admin）+ 余额查询 `GET /credit/balance` + LoginUserVO.credits；`CreditService` 冻结/结算/退款（冻结额 = 基础价 × 生成类型系数 × 强度档系数，`agent.credit.*` 配置，余额不足 → 40201「积分不足」）；内部端点 `POST /internal/agent/runs/{runId}/credit/freeze`（Bearer + runId 幂等 + wireframe_confirmed 前置，冻结后 creditLedgerRef 写回 run）。② **completeRun 三剧本记账**——success → 结算（SETTLED）+ 构建 + run=done；failed → 全额退款（REFUNDED）+ 错误历史 + run=failed；aborted → 折算退款（首文件落盘前 filesWritten≤0 全额 REFUNDED，已写文件按里程碑 PARTIAL_REFUNDED：里程碑≥3 结算 70%、否则 50%）+ 历史带 `[用户中断]` 标记 + run=aborted；run 终态与台账状态一致（markRunTerminal 兜底）。③ **TS Agent 对话中断（架构 §3.5 中止 (a)）**——`/agent/stream` 感知客户端断开（`reply.raw.on('close')` + writableEnded 判断）→ `abortController.abort()` → workflow 传 `abortSignal` 给 streamText/generateText + error part 区分 AbortError + `throwIfAborted` 关键决策点检查（工具执行后调用间隙）→ `GenerationAborted` → **abortRun 中断终态**（保留已写文件 + run=aborted + 回调 aborted/filesWritten）；`FileTools.filesWritten` 计数（首文件落盘阈值）。④ **冻结时点 = 进入 codegen**——闸门通过后、workflow 前调 `runClient.freezeCredit`，余额不足（402）→ 唯一 error 事件拒绝（零 token 消耗），幂等不重复扣。**e2e 实测根因修复**：failed 回调 ai 消息为空 → Java completeRun 写历史抛「消息不能为空」阻断退款 → 跳过空消息（failed 分支另写错误历史交代）。验收证据：Java 相关测试 **70/70**（CreditServiceImpl 17 + GenerationRunServiceImpl 31 + 两个 Controller 22）；TS **type-check** + **137/137**（原 128 + abort 3 + 冻结 2 + runClient 3 + filesWritten 1）；全量 mvn 对比基线 97fb2d8（81 测 = 18 errors + 1 failure）→ 当前 115 测 = **同 18 errors + 1 failure**（无回归，均为既有环境性失败：langgraph4j/图片 API/截图/DB 驱动）；build 2.4MB；`git diff --check` 通过。**WSL e2e 三剧本 + 两附加验收**（MySQL+Redis+Java(8123, INTERNAL_API_TOKEN=dev-token)+TS Agent(8092) 实跑，用户注册→SQL 充值 500→SQL 插 html 应用→`/app/agent/token` 取 JWT→访谈→线框→确认→stream）：**完成**（单页线框避免视觉 diff 覆盖不足）run=done + 台账 **SETTLED**（frozen 100/settle 100/refund 0/complete）+ 余额 400 + 历史落库；**失败**（script=error）run=failed + 台账 **REFUNDED**（refund 100/failed）+ 余额退回；**中断-已写文件**（内部 freeze + 模拟落盘 + completeRun aborted filesWritten=1）run=aborted + 台账 **PARTIAL_REFUNDED**（settle 50/refund 50/interrupted）+ 余额 350 + 历史 ai = `[用户中断] 生成已中断，已保留 1 个已生成文件` + index.html 保留；**中断-首文件前**（filesWritten=0）台账 **REFUNDED**（refund 100/interrupted）+ 余额全额退回；**幂等**（重复 completeRun/freeze 均 200 不重复记账，台账 1 条）；**余额不足**（SQL 置 0）stream → 唯一 error「积分不足，当前余额 0，本次生成需 100 积分，请先充值」+ 台账 0 条。验收 5 条逐项勾选并关闭 Issue #10。commit：`fb63148`（Java 台账+三剧本记账）/ `25cb132`（TS 冻结+中断）/ `707bf5d`（空消息修复）。


- **#9 质检门禁 + 输出护栏 + 三档强度 + token 计量完成（Issue #9，2026-09-05）**：① **三工位循环 + 三重门禁**——machine.ts coding↔review 有界重试（`MAX_QUALITY_RETRIES=2` + 共享 `MAX_QUALITY_ATTEMPTS=3` 消除双源；重试次数经 `context.qualityAttempts` 计数，里程碑区分「规划页面结构/根据质检意见重新生成」「检查生成结果/复查生成结果」）；reviewer（`src/review/`）依序三道门禁——`QualityScoreGate`（generateText + `scripted-quality` 模型 + code-quality-check 提示词，结构化质检分 isValid/errors/suggestions，解析失败视为未通过，suggestions 经 QualityScoreGate.detail 透传回喂 coder system）+ `DefaultBuildVerifier`（html 静态校验 index.html 含 `<html>` 根；multi_file/vue_project 明确提示「L1 构建管线未接入」占位，Agent 侧 build 门禁 MVP 语义）+ `DefaultVisualDiffVerifier`（**基准 = 已确认线框**：路由从 `run.context.wireframe.relativeUrl` 解析绝对路径经沙箱校验传入 `options.wireframePath`，结构启发式对比 page-N 区段覆盖；无基准 → 失败宁可重试；Playwright 渲染级 diff 属远期架构 §6）；`runReviewGates` 汇总 errors（`【门禁名】detail`）/suggestions（去重）。② **护栏前三层 + 三档强度**——`src/intensity.ts` INTENSITY_TIERS fast/standard/deep（标准档 maxTurns=3/maxOutputTokens=8000/maxToolCalls=50（先例）/maxImages=4；fast/deep 随档位缩放；价格系数 ×1/×1/×2 预留）；streamText 传 `maxOutputTokens` + `stopWhen:[isStepCount(maxTurns), stopWhenToolCalls(maxToolCalls)]` 双护栏；**超限优雅收尾**（绝不硬杀）：finishReason 为 `tool-calls`（工具护栏截断）**或 `length`**（token 护栏截断）→ generateText 注入收尾指令让模型输出完整交代 + ai_thinking「已达本次生成硬上限，正在收尾」→ 状态机仍按拓扑推进（PROCEED→review→PASS→done）不卡 coding（修正：PASS 在 coding 无转移定义会被静默忽略，必须先 PROCEED 进 review 再 PASS）；输入历史滑窗 `src/workflow/history.ts`（最近 10 轮全文 + 更早折叠摘要并入 system，`windowedHistoryOf` 单次计算供消息组装与 system 共用）。③ **token 计量**——每轮 streamText/generateText usage 累计（字段可能 undefined 按 0）+ 质检门禁 usage 经 GateResult.usage 透传，终态前 `runClient.updateRun({ tokenUsage })` 落库（done/failed 都落）。④ **三档路由**——`provider.languageModel(resolveModelId(tier))`；config 增 `MODEL_FAST/MODEL_STANDARD/MODEL_DEEP` 覆盖（.env.example 已加注释占位）；假 LLM 注册四模型（scripted-fast/standard/deep/quality）+ `records` 数组供断言路由与上限（modelId/maxOutputTokens/hasToolResult）。请求体新增 `intensity`/`history`/`codeGenType`（白名单校验，非法回退 html；script 6 分支嵌套三元改 `SCRIPT_WHITELIST` 查表）。验收证据：`bash scripts/run-wsl.sh type-check` 通过；`bash scripts/run-wsl.sh test` → **128/128 passed**（原 86 + intensity 5 + history 6 + review 18 + quality-gate 13）；`bash scripts/run-wsl.sh build` → `wsl-rt-env/ts-agent/dist/app.bundle.mjs` 2.3MB；`git diff --check` 通过。**code-review 双轴审查整改闭环**（5 个 commit：`4b3ae4e` feat 主体 / `f80d1e3` 超限收尾状态机推进 / `467071d` 验收级测试真实剧本 + 三档路由断言 / `530fb90` 质检 token 计入 run 计量 + route 质检剧本 / `5420ca2` script 查表 + codeGenType 收紧 + 删死代码）；Standards 轴 0 硬违规、8 项基线臭味整改；Spec 轴 c1（假产物无 page-N 区段 → 假 LLM 产物加 `<section class="page" id="page-N">`）、a2（max_output_tokens 截断未判 → finishReason 补 length）等关键发现闭环；「npm run build 门禁为占位」为有意 MVP 取舍（主链路 html L0 静态部署无 npm 项目；multi_file/vue_project 待 L1 构建管线），非缺陷。**踩坑**：AI SDK 的 `PASS` 事件在 coding 状态无转移定义（send 被静默忽略）→ 超限收尾必须 PROCEED 进 review 再 PASS；vitest 默认 5s 超时对成功剧本（3-5s）全量并行偏紧 → `vitest.config.mjs` testTimeout 提到 20s（真死循环仍会暴露）。
- **#9 code-review 双轴第二轮整改闭环（2026-09-05，补跑审查后全部判断项）**：Standards 轴 0 硬违规 + 8 判断项、Spec 轴 5 发现全部处置——① **Middle Man 并层**：`BuildGate`/`VisualDiffGate` 纯透传类删除，`DefaultBuildVerifier`/`DefaultVisualDiffVerifier` 直接实现 `ReviewGate`（含 name），`gatesFromSet` 简化为 `[QualityScoreGate, set.build, set.visualDiff]`；② **死代码**：删除 `HTML_EXTENSIONS`（5420ca2 漏掉的新增文件）；③ **魔数**：`GATE_NAMES` 常量（quality-score/build/visual-diff）消除 workflow/测试三处字面量；④ **Repeated Switches**：route 的 codeGenType 三元链改 `CODE_GEN_TYPE_WHITELIST` 查表（与 SCRIPT_WHITELIST 风格一致）；⑤ **Data Clumps**：`QualityScore.usage` 用共享 `TokenUsage`，`accumulateUsage` 参数改 `Partial<TokenUsage>`；⑥ **Duplicated Code**：llm limit 剧本带/不带工具结果两分支合并构造；⑦ **Mysterious Name**：`QualityScore.score` → `grade`（消除 `score.score` 同形读法）；⑧ **Divergent Change**：review 编排抽离为 `runReviewCycle`（review/index.ts：上下文构建/门禁执行/质检 token 计量回调），workflow 只做参数组装；⑨ **Spec c1**：超限收尾 `generateText` 的 system 拼入已生成 `pageContent`（此前只传原始 messages，真实 provider 无法基于产物交代）；⑩ **Spec c3**：超限路径只 PROCEED、PASS 统一由 done 段发送（消除 actor 终态后重复 send 的 XState 告警）；⑪ **Spec c4**：新增 `limit-length` 剧本（finishReason=length 模拟输出截断）覆盖 length 截断优雅收尾路径（此前仅 tool-calls 有测试）；⑫ **Spec c2**：超限绕过三重门禁的取舍在 contract.md 明示（已达上界重试必再触限，MVP 成本上界优先）。命令证据：`type-check` 通过；`test` **128/128**（删 BuildGate 透传 1 例 + 增 limit-length 1 例，净持平）；`build` 2.3MB。commit `10b9a97`。

## 2026-09-03

- **P0 止血执行完毕（Issue #2）**：`application-local.yml` 回切 `python-agent.enabled: false`；PG 停用确认（无服务、无自启机制、5432 无监听）；WSL 全栈旧 Java AI 链路 e2e 全流程验证通过——注册/登录（Redis 会话）→ 建应用（`/api/app/add`）→ SSE 生成（旧链路直出 ~80KB 完整 HTML + 终端 `event:done`）→ 落盘 `tmp/code_output/html_453132478241230848/` → 构建部署（deployKey `VYvF03`、deployedTime 落库）→ 对话历史落 MySQL。运行环境：用户态 MySQL 8.0.46（`~/.local/opt/mysql8/start.sh`，需 DSH 完整权限）+ redis-server 7.2.5（源码编译，无持久化）+ `./mvnw spring-boot:run`（需 DSH 完整权限写 `~/.m2`）。
- **目录名复用决策 + 记忆重构（用户指令）**：`git mv paimeng-ai-code-agent paimeng-ai-code-rag`（60 个跟踪文件随迁，旧 Python Agent 代码成为 RAG 骨架复用起点，取代"新建后删除"方案）；TS Agent 落位 `paimeng-ai-code-agent/`。记忆侧：删除 `.agents/memories/python-agent.md`，新建 `ts-agent.md` / `python-rag.md`（Python 实测契约教训迁移至 ts-agent.md「移植要点」）；同步 `MEMORY.md`、`.agents/memories/{architecture,java-backend,deployment,README}.md`、`AGENTS.md`、`CONTEXT.md`、`docs/ts_agent/architecture.md`（§1/§10.3 修订）、`.agents/skills/project-startup-guardrail/SKILL.md`（启动模式去 Python 化、PG 标注停用、8090→8092）。
- **#3 TS Agent 骨架完成（Issue #3）**：`paimeng-ai-code-agent/`（Fastify 5.12 + TypeScript 5.9 + jose 6.2 + vitest 3.2）。验收证据：`npm run type-check` 通过、`npm test` **24/24 passed**（healthz / 鉴权 401 矩阵×8 / 沙箱逃逸×9 / SSE 顺序与格式×6）；curl 实测——`/healthz` 200 `{"status":"ok"}`，`/agent/*` 无令牌/乱串/过期令牌均 401，合法令牌（HS256，`appId` 载荷 + `sub`）访问真实 P0 工作区 `tmp/code_output/html_453132478241230848` → 200，`../..` 逃逸 / `/etc` 外部路径 / 相对路径 → 400，`GET /agent/smoke/sse` 输出 milestone→ai_thinking→ai_response→tool_request→tool_executed→milestone→done 七帧（`event:` + 单行 JSON `data:` + 空行分隔，tool_request 先于同 id tool_executed，done 终态收尾）。JWT：HS256 共享密钥 + algorithms 白名单 + `requiredClaims:['exp','sub']`，离线验签。默认 `WORKSPACE_ROOT` 解析 `<repo>/tmp/code_output`。测试基建：`buildApp(overrides)` + `fastify.inject()` + `jose SignJWT` 自签（`test/helpers.ts`，后续票据沿用）。
- **运行时环境合规整改 + AGENTS.md 瘦身（用户指正）**：TS Agent node_modules 迁入 `wsl-rt-env/ts-agent/node_modules`（服务目录内改符号链接，`npm install`/`npm test` 命令不变，24/24 复测通过），构建产物 `npm run build` 输出 `wsl-rt-env/ts-agent/dist`（tsconfig.build.json outDir 调整）；根 `.gitignore` 补 `wsl-rt-env`（存量待办）。**AGENTS.md 按渐进式披露瘦身**（81→40 行）：三服务职责/过渡态/鉴权拓扑/数据分工等重复内容删除，改为「按任务读权威文档」路由表 + 常用命令 + 5 条硬约定；迁出的 Java 包结构/MyBatis 生成器约定承接进 `java-backend.md`。教训：存量豁免（"迁移完成前按实际路径执行"）不得泛化到新项目，新运行时环境必须直接落 `wsl-rt-env/`；AGENTS.md 只做路由与硬约定，领域知识一律指向权威文档。
- **npm 符号链接破坏行为实测（历史方案，已废弃）**：曾验证 `npm install` 的 reify 会替换 node_modules 符号链接。该阶段的“归位”实现与 `npm install && ...` 流程已删除；当前 WSL 方案改为 `paimeng-ai-code-agent/scripts/install-wsl-node-modules.sh` 通过 `npm --prefix wsl-rt-env/ts-agent install` 直接安装到运行时目录，不产生服务目录依赖、迁移操作或软链接。

## 2026-09-04

- **#4 generation_run 生命周期完成（Issue #4）**：① `sql/create_table.sql` 新增 `generation_run` 表（run_id varchar PK 复用 runId 语义、phase **显式 MySQL ENUM** 九值 interview→…→done/failed/aborted、context/milestones/token_usage JSON、credit_ledger_ref 预留、started/finished_time、idx_appId_phase/idx_userId）——已实库落表（`SHOW CREATE TABLE` 验证）；② Java 内部 API（`GenerationRunController`，`/api/internal/*`，Bearer 服务令牌 `internal-api.token`）：`POST /internal/runs`（同 runId 幂等返回既有；同 app 非终态并发 → **409**「当前有进行中的任务」）、`PATCH /internal/runs/{runId}`（无变化不落库；进终态自动补 finished_time）、`GET /internal/runs/{runId}`、`GET /internal/apps/{appId}/runs/latest-nonterminal`；无/错 Bearer → **401**（控制器内 `@ExceptionHandler` 覆盖全局 200 返回，参数→400、不存在→404）；③ TS Agent run 客户端 `src/internal/runClient.ts`（create/update/getRun/getLatestNonTerminalRun + `RunConflictError`/`RunApiError` 错误映射）+ 配置 `JAVA_INTERNAL_BASE_URL`/`JAVA_INTERNAL_TOKEN`。验收证据：`./mvnw test -o -Dtest='GenerationRunServiceImplTest,GenerationRunControllerTest'` **19/19**（service mock mapper 12 例含并发线程安全 + controller standalone MockMvc 7 例 401/409/404/200）；`npm run type-check` 通过、`npm test` **33/33**（原 24 + run-client 9）。DB 实库验证 + curl 冒烟（含 401/409/幂等）见本票收尾记录。
- **Java 构建产物迁入 wsl-rt-env（用户指正）**：`target/` 迁入 `wsl-rt-env/java/target`，`pom.xml` 暴露 `maven.build.directory` 属性（默认 `${project.basedir}/target`）并让 `<build><directory>` 引用它——WSL 命令 `./mvnw -Dmaven.build.directory=$PWD/wsl-rt-env/java/target ...` 指定，**不建软链**。教训：`-Dproject.build.directory` 是模型派生属性不可靠（surefire 等插件不认，会重建根 `target/`，实测确认）；正确做法是 pom 自有属性 + 命令行 `-D`。证据：`./mvnw -o -Dmaven.build.directory=$PWD/wsl-rt-env/java/target test -Dtest='GenerationRunServiceImplTest,GenerationRunControllerTest'` **19/19** 且根 `target/` 不再重建；`spring-boot:run` 同属性启动 → 8123 起、内部 API 401/创建/latest 冒烟通过。文档同步：AGENTS.md / deployment.md / java-backend.md / MEMORY.md / startup-guardrail SKILL.md。前端 node_modules、Python `.venv` 仍待迁移。
- **TS Agent 去 symlink：esbuild 打包改造（用户指令「通过命令指定运行环境，不要通过软链」）**：服务目录内 node_modules 符号链接移除，改造为 esbuild 自包含打包方案——① `npm run build` 产出 `wsl-rt-env/ts-agent/dist/app.bundle.mjs`（`--format=esm --platform=node` + banner 注入 `createRequire`，fastify 内部 CJS require 才能跑；CJS 格式会挂 `import.meta.url`，实测排除）；② `npm start`/`npm run dev` 经 `scripts/run.mjs` 调度器运行 bundle 并注入 `AGENT_ROOT` 环境变量（`src/config.ts` 据此定位 `.env`/`WORKSPACE_ROOT` 基准——bundle 在 wsl-rt-env，无法从 `import.meta.url` 反推）；③ dev = esbuild watch 重建 + `onEnd` 回调重启应用（**DrvFs 上 `node --watch` 收不到文件事件，实测 2 次源码变更 0 次重启**，esbuild 自带轮询兜底可收到）；④ 测试 `vitest.config.mjs`（resolve.alias 指向 wsl-rt-env 的 fastify/jose/vitest + cacheDir 移入 wsl-rt-env 防止服务目录催生 node_modules）；⑤ 类型检查 `tsconfig.json` paths 显式映射三个包的 d.ts **文件**（NodeNext 下 paths 到目录无效；typeRoots 双候选本地优先）+ 删 `tsconfig.build.json`；⑥ 归位脚本去软链化（本地暂存安装 → mv 进 wsl-rt-env，不再重建链接），`tsx` 依赖移除、`esbuild` 转直依赖。**踩坑链**：Node ESM 不认 NODE_PATH；esbuild JS API 不读 NODE_PATH（CLI 才读）→ 用 `nodePaths` 选项；esbuild `bin/esbuild` 可能被 postinstall 换成原生 ELF（不能 `node bin/esbuild` 调用，走 `lib/main.js` JS API）。验收：`npm test` **33/33**、`npm run type-check` 通过、`npm run build` 产物 1.4MB、`npm start` healthz 200 + `.env`（PORT=8093/JWT_SECRET）生效 + jose 鉴权 401/200、`npm run dev` 变更重启验证（PID 变化）。文档同步：AGENTS.md / deployment.md / ts-agent.md / SKILL.md / README.md。前端 node_modules、Python `.venv` 仍待迁移。
- **全仓库 WSL 运行时迁移完成（用户指令）**：前端新增无软链调度器与归位脚本，`node_modules`、Vite cache、构建产物分别位于 `wsl-rt-env/frontend/`；Python RAG 新增 uv 环境同步与运行脚本，Python 3.14、venv、uv cache 均位于 `wsl-rt-env/python/`。前端 WSL `npm run build-only` 成功（5,605 modules，产物在 WSL runtime）；Python `bash scripts/run-wsl.sh test tests/test_guardrails.py` **6/6 passed**。前端 `npm run type-check` 与完整 `npm run build` 均通过。Windows 使用服务目录本地依赖，调度器绝不跨平台回退，原有 IDE 运行配置不变。
- **Issue #5 最小生成流 + SSE 契约定稿完成（2026-09-04，重做后定稿）**：`POST /agent/stream` 按票据要求落地 **XState v5 线性工作流 + Vercel AI SDK provider 抽象**（此前手写 generator 版本被用户重开）——① 新增 `src/machine.ts`：XState 状态机 `interview → coding → review → done / failed`，非法转移类型层消灭，milestone 经 entry action 聚合进 `context.milestones`（节点跳变 → 人话里程碑，退款锚/run 更新复用）；② 新增 `src/llm.ts`：脚本化假 LLM 以 AI SDK `LanguageModelV2` 接口实现、经 `customProvider` 注册为 `scripted` 模型（零在线调用），`src/workflow.ts` 用 `streamText` + `writeFile` 工具 + `stopWhen: isStepCount(2)` 驱动工具循环（AI SDK v7 已无 `maxSteps`），`fullStream` part 映射为 SSE 事件（text-delta→ai_response、tool-call→tool_request、tool-result→tool_executed、error part→抛出走 failed）；③ 成功路径事件序列 `milestone→ai_thinking→milestone→ai_response×2→tool_request→tool_executed→ai_response→milestone×2→done`，tool_request 先于同 id tool_executed；error 剧本终止于 error（后无业务事件）且 run→failed；run phase 随状态机推进 interview→coding→review→done。新增 `docs/ts_agent/contract.md`（契约定稿）与 `test/stream.test.ts`（按语义断言）。运行时配套：`ai`/`xstate`/`@ai-sdk/provider` 依赖落 wsl-rt-env；tsconfig `paths` 与 vitest `resolve.alias` 补映射；**vitest 测试改走 `--configLoader runner`**（服务目录无 node_modules 时 vite 上溯到 `$HOME/node_modules/.vite-temp` 写配置，被 Windows ACL 锁死实测 EACCES，runner 模式不打配置包、彻底绕开）。命令证据：`bash scripts/run-wsl.sh type-check` 通过；`bash scripts/run-wsl.sh test` → **37/37 passed**；`git diff --check` 通过。
- **WSL shell 启动入口统一（用户指令）**：Java 新增根目录 `scripts/run-java-wsl.sh`（`run`/`test`/`compile`）；TS Agent、前端分别新增模块内 `scripts/run-wsl.sh`（默认 `dev`，支持各自 npm 子命令），Python RAG 复用并强化既有 `scripts/run-wsl.sh`；所有 WSL 安装/运行脚本均检查 Linux/WSL。验收：通过 wrapper 实测 Java **19/19**、TS Agent **33/33** + type-check、前端 type-check + build、Python RAG **6/6**。AGENTS.md、deployment/领域记忆、README、handoff 与启动 SOP 已同步。
- **#5 代码审查整改（/code-review 后处理）**：修复 Standards 硬违规（`src/routes/agent.ts` 两处英文注释改中文）+ 收敛 3 条「复述做什么」注释（`llm.ts` 79/94 行、`workflow.ts` 79 行改为「为什么」/纯分支标记）；Spec 结构兜底——`/agent/stream` 路由对 `done` 补对称终态守卫（done/error 都是流的最后一个事件，收到任一即停止消费，error 后不再可能带进误发 done）；去冗余——`workflow.ts` 将 `lastPhase` 初始化为 actor 初始 phase（路由 `createRun` 已写 interview），首次 `sync()` 不再重复 `updateRun(interview)`，interview 里程碑照常经 SSE 发射。测试断言同步（run phase 序列去掉重复 interview）。命令证据：`node scripts/run.mjs type-check` 通过；`node scripts/run.mjs test` → **37/37 passed**。
- **#6 Agent→Java 回调打通完成（Issue #6，2026-09-04）**：① **Java 侧泛化**——`ai/python/*` 三类（`PythonAgentClient`/`PythonAgentSseAdapter`/`RunIdSinkRegistry`）整体迁移泛化为 `ai/agent/*`（`AgentClient`/`AgentSseAdapter`/`RunIdSinkRegistry`，`AgentRequest`/`AgentCallbackRequest`/`AgentProperties`），旧类删除；配置段 `python-agent.*` → `agent.*`（expand-contract：`AgentLegacyAliasPostProcessor` EnvironmentPostProcessor 把旧键复制到新键，旧配置别名保留，`application.yml` 保留 `python-agent` 段作旧别名；`application-local.yml` 注释更新）。② **完成回调链路**——新增 Java 内部端点 `POST /internal/agent/runs/{runId}/complete`（`GenerationRunController` + `GenerationRunService.completeRun`）：Bearer 服务令牌（无/错→401）、runId 内存幂等（`completedRunIds`，重复回调 200 丢弃不重复写/构建）、写本次对话历史（messages user/ai 按序落 `chat_history`，appId/userId 字符串防 JS 精度丢失）、success 按 app.codeGenType + workspacePath 触发 `BuilderExecutor.doBuild`（构建管线不变）、failed 写一条错误历史。③ **TS Agent 侧**——`runClient.completeRun()`（POST complete）+ `workflow.ts` `notifyComplete()`：终态事件（done/error）**先于 yield** 回调 Java（路由收到终态即 break、generator 不再 resume，yield 后的代码不执行——踩坑实证）；route 注入 JWT sub 兜底 userId；回调失败不阻断主流程。**验收证据**：`mvn test`（#6 相关 6 类 46 例：AgentClient 5/AgentSseAdapter 3/RunIdSinkRegistry 6/别名 3/Controller 11/ServiceImpl 18）**全绿 BUILD SUCCESS**；TS Agent `type-check` + `test` **37/37**；全量 mvn 的 9 个失败（截图/LLM guardrail/外部工具 API）经 **git worktree 基线 a14b094 对比确认既有失败、非本票回归**；WSL e2e 全链路——起 MySQL+Redis+Java(8123)+TS Agent(8092)，自签 JWT 调 `/agent/stream`（假 LLM success）→ SSE 完整事件序列 + `chat_history` 落库 user+ai 两条 + 工作区 `index.html` 产物 + `generation_run` phase=done + 重复回调幂等不重复写 + 无/错 Bearer 401 实测通过。**回退链路**：`python-agent.enabled=false` 经别名复制到 `agent.enabled=false`，Java 服务正常启动、旧 Java AI 分支代码未动（仅开关变量名切换，语义等价）。文档同步：`docs/ts_agent/progress.md` / `.agents/memories/{java-backend,ts-agent,architecture}.md` / `MEMORY.md`。
- **#7 需求工程完成（Issue #7，2026-09-04）**：TS Agent 新增五维访谈 + 免费线框 + 确认闸门（P2 前半程，票据 #7 验收全过）。① **访谈**（`src/interview.ts` + `POST /agent/interview`）：固定 5 维（受众/风格/页面清单/数据需求/交互），每维脚本化生成 2-4 选项选择题，`MAX_INTERVIEW_ROUNDS=2`；各维均作答即收束（**跳过剩余轮次**），缺信息才第 2 轮只追问缺失维度；状态持久化于 `run.context.interview`（跨请求存活）；已收束幂等重放返回结论。② **线框**（`src/wireframe.ts` + `POST /agent/wireframe`）：快速档脚本化单文件 HTML（灰块 + 占位图 + 页内锚点可点击跳转 + **站点地图**，`PAGE_LIMIT=5`），落 `{workspace}/wireframe/wireframe.html`，run → `wireframe_pending`；**免费**（不扣积分）但生成前调 Java 配额端点，超限 → **429** 明确报错。③ **确认**（`POST /agent/wireframe/confirm`）：`wireframe_pending → wireframe_confirmed`（幂等，积分冻结时刻挂点 #10）。④ **codegen 闸门**：`/agent/stream` 先经 `runClient.getRun` 校验，非 `wireframe_confirmed` 输出唯一 `error` 事件「未确认线框，无法进入代码生成（{阶段}）：请先完成访谈并确认线框」，不发业务事件；确认后正常走 workflow（wireframe_confirmed → coding → review → done）。⑤ **Java 配额端点**：`POST /internal/agent/wireframe/quota/acquire`（`GenerationRunServiceImpl.acquireWireframeDailyQuota`，**复用 RateLimitAspect 的 Redisson 令牌桶机制**：`rate_limit:user:{userId}:wireframe_daily` + OVERALL + 86400s 滚动窗口 + tryAcquire，键 TTL 25h；不直接套 `@RateLimit` 注解因为其 USER 类型靠 servlet session，内部 Bearer 端点改用请求体 userId 键控）；配置 `agent.wireframe-daily-limit`（默认 10）。命令证据：TS `type-check` 通过、`npm test` **48/48**（原 37 + requirements 8 + stream 闸门 3）；Java `./mvnw -o -Dmaven.build.directory=... test -Dtest='GenerationRunServiceImplTest,GenerationRunControllerTest'` **35/35**（service 21 + controller 14）；全量 `compile` BUILD SUCCESS、`git diff --check` 通过。**WSL e2e 全链路验收**（起 MySQL+Redis+Java(8123, INTERNAL_API_TOKEN=dev-token)+TS Agent(8092)，自签 JWT）：访谈第 1 轮 5 题 → 全答即收束（跳过第 2 轮）→ 线框生成（单文件 HTML、站点地图 3/5 页、落盘 wireframe/）→ run 经 Java 内部 API 持久化 `wireframe_pending`（跨请求存活）→ **未确认时 codegen 被闸门拒绝**（error 明确报错）→ 确认 `wireframe_confirmed`（幂等）→ codegen 放行至 done + 工作区 index.html + `generation_run` phase=done/milestones 4 + `chat_history` user/ai 两条落库（真实 app）；部分作答 → 第 2 轮只追问缺失 3 维；不存在的 run 直接 codegen 被闸门拒；配额端点连续 11 次 → 第 11 次 **429**。**测试基建坑**：`.env` 的 `JAVA_INTERNAL_TOKEN` 被 `loadDotEnv` 读进 config，`buildTestApp` 必须显式 `javaInternalToken: ''` 强制离线（已加），否则测试误连真实 Java。文档同步：`docs/ts_agent/contract.md`（增补需求工程端点 + 闸门）、`paimeng-ai-code-agent/README.md`、`.agents/memories/{ts-agent,java-backend}.md`、`MEMORY.md`。
- **#7 代码审查整改（/code-review 后处理，2026-09-04）**：修复 Standards 1 处硬违规（`GenerationRunServiceImpl.acquireWireframeDailyQuota` 是 `@Override` 却带重复 Javadoc 且首行以句号结尾 → 删 Javadoc、机制 rationale 挪入方法体行内 `//` 注释，接口文档保留）+ Spec 2 项：① **重新访谈失效旧线框**——`/agent/interview` 在 `wireframe_pending` 阶段被再次调用时回到 `interview` 并清空 `context.wireframe`（防旧线框被确认成与新需求不一致的布局契约；本地 `context` 同步 `delete`，避免后续 persist 重新写入）；② **闸门不再静默放行**——`/agent/stream` 未配置 Java 内部 API 时输出唯一 `error` 事件「Java 内部 API 未配置，无法校验线框闸门，拒绝进入代码生成」，与 interview/wireframe/confirm 的 503 口径一致（不再让 codegen 离线绕过闸门）；契约文档增补两条语义。测试：TS 新增 2 例（stream 未配置 API 拒绝 1 + requirements 重新访谈失效线框 1）→ **50/50**；Java 35/35 复测通过。命令证据：`bash scripts/run-wsl.sh type-check`、`bash scripts/run-wsl.sh test` → 50/50、`./mvnw -o -Dmaven.build.directory=... test -Dtest='GenerationRunServiceImplTest,GenerationRunControllerTest'` → 35/35、`git diff --check` 通过。
- **#8 生成核心移植完成（Issue #8，2026-09-04）**：① **Guardrail**（`src/guardrails.ts`，从 Python `guardrails.py` / Java `PromptSafetyInputGuardrail` 逐条移植：长度 1000 / 空输入 / 敏感词 9 个 / 注入模式 5 条正则，`validatePrompt`）；workflow 在 interview 阶段（coding 前）校验，拒绝 → failed 终态 + 明确报错 + 不进入 coding（machine interview 补 FAIL 转移）。② **代码块解析**（`src/codegen/parsing.ts`：`parseHtmlCode`/`parseMultiFileCode`/`toFiles`，正则与 Python 逐字对齐）；writeFile 写盘前经解析出的文件集落盘（`src/tools/index.ts` 的 `fileContentByPath`）。③ **文件六工具**（`src/tools/fileTools.ts`：writeFile/readFile/modifyFile/deleteFile/readDir/exit + IGNORED_NAMES/EXTENSIONS/IMPORTANT_FILES 重要文件保护 + 沙箱路径越界 FilePathError）。④ **图片四工具 + 配额**（`src/tools/imageTools.ts`：Pexels 内容图 / Undraw 插画 / DashScope Logo / mmdc 架构图，HTTP 客户端与渲染器可注入；**图片配额 4 张/run**——`acquire` 截断到剩余配额、用尽后返回明确报错 `IMAGE_QUOTA_EXCEEDED_MESSAGE`）。⑤ **提示词 7 份复用**（复制进 `src/prompts/`，`src/prompts.ts` 经 AGENT_ROOT 加载；codegen 三份注入「导览组件强制产出」要求，架构 §9 onboarding 生成进应用本身）。⑥ **workflow 全套工具注册**（`src/tools/index.ts` buildTools：文件六 + 图片四 = 10 工具，`streamText.tools` 注入 + codegen 提示词作 system）。⑦ **fake LLM 扩展**（`buildPageContent` 产物内嵌 onboarding-tour 应用内组件；新增 `images` 剧本一轮并行 2 次图片搜索触发配额拒绝）。验收证据：`bash scripts/run-wsl.sh type-check` 通过；`bash scripts/run-wsl.sh test` → **85/85 passed**（原 50 + guardrails 6 + parsing 6 + fileTools 12 + imageTools 7 + stream #8 4）；`git diff --check` 通过。验收逐条：工具成对事件+落盘（成功剧本 + images 剧本按 id 配对断言）、图片配额被拒（第 1 次取满 4 张第 2 次拒绝）、Guardrail 6 用例、解析 6 用例语义等价、产物含导览组件（index.html 含 `onboarding-tour`）。配置新增 `PEXELS_API_KEY`/`DASHSCOPE_API_KEY`/`IMAGE_MODEL`（.env.example 已加）。
- **#12 前端通道切换完成（Issue #12，2026-09-04）**：对话页从旧通道（Java 中转 SSE + cookie）切换到新通道（fetch-SSE + JWT 直连 TS Agent）。① **Java 签发侧**——`ai/agent/AgentJwtProperties`（`agent.jwt.secret`/`agent.jwt.ttl-minutes`，默认 10 分钟，空密钥拒签）+ `AgentJwtService`（hutool JWT HS256，声明 `sub`=字符串 userId（防 JS 精度丢失）/`iat`/`exp` 整秒时基）+ `AgentTokenVO`；端点 `GET /app/agent/token?appId=`（登录校验 + 应用归属校验「无权限生成代码」 + codeGenType 校验 + **workspacePath 由 Java 计算**（`CODE_OUTPUT_ROOT_DIR/{codeGenType}_{appId}`，浏览器不感知服务器布局））。② **前端**——`src/utils/agentSse.ts`（fetch 流式 SSE 解析器：空行分帧、多行 data 重组、type 与 event 一致性校验、CRLF 归一、AbortSignal、终态 done/error 返回值、`AgentStreamHttpError` 携带状态码）；`src/api/agentToken.ts` 手写 API（非生成链路）；`AppChatPage.vue` 重写 `generateCode`（登录态换 token+workspacePath → `createRunId()` → 直连 `/agent/stream`），**七类事件渲染**——ai_response 进 Markdown、ai_thinking 折叠面板（a-collapse）、milestone 蓝绿 tag 进度条、tool_request/tool_executed 按 id 配对（Loading/Check 图标 + 中文工具名 + relativeFilePath）、done 刷新预览（回调先于 done，构建已完成）、error 明确报错；**401 明确提示重新登录并跳转登录页**（非挂起）；onUnmounted abort。③ **路由**——vite 代理新增 `/agent → localhost:8092`；生产模板 `deploy/nginx.conf.example`（/api→8123、/agent→8092，SSE 禁缓冲 + 长 read timeout）。④ **前端 lint 环境修复**——run.mjs WSL 环境注入 `NODE_PATH`（eslint.config.ts 顶层裸导入无法从 wsl-rt-env 布局解析，lint 在该布局下首次可运行）；dev/preview/build-only 加 `--configLoader runner`（vite bundle 模式写 `$HOME/node_modules/.vite-temp` 被沙箱/Windows ACL 锁死 EROFS，与 vitest 同源踩坑）；`src/api/**` 生成文件豁免 lint（生成模板 `@ts-ignore` 头过不了 ban-ts-comment）；存量 lint 错误清理（GlobalHeader 未用变量、loginUser/UserLoginPage/visualEditor/AppChatPage 的 any 收紧、@ts-ignore→类型化）。⑤ **验收证据**——Java：新增 `AgentJwtServiceTest`（签发验签/声明齐全/大 id 字符串/过期 ValidateException/错密钥拒绝/空密钥拒签 6 例）+ `AppControllerAgentTokenTest`（未登录 40100/应用不存在 40400/非归属 40101/归属 200 且验签+sub 字符串+路径命名 4 例）**10/10 全绿**；全量 mvn 的 10 个失败经 stash 基线核实为旧 AI 链路既有不稳定（LLM guardrail/图片 API），非本票回归；前端 `type-check` + `lint` 全绿（WSL）。**WSL e2e 全链路**（MySQL+Redis+Java+TS Agent 实跑）：注册/登录（cookie）→ `GET /app/agent/token`（TTL 10min）→ 无/错 Bearer 401 → 按 #7 契约完整走「interview 5 题→answers 收束→wireframe_pending→confirm wireframe_confirmed→stream」，**七类事件全部按契约出现**（milestone×4/ai_thinking/ai_response×3/tool_request→tool_executed 同 id 配对/done），run phase=done+finishedTime、chat_history user+ai 落库、index.html+wireframe.html 落盘；再经 vite 代理（5173）全流程复测一致。踩坑记录：①`generation_run` 并发检查——被闸门拒绝的 run 停在 interview 非终态会挡后续 createRun（409「当前有进行中的任务」），测试残留需 PATCH phase=failed 清理；②JSON Long 全字符串序列化（expiresAt 也是字符串，前端解析需 int()）；③e2e 必须走 #7 需求工程闸门，直接 stream 会被拒（预期行为）。文档同步：progress/记忆（vue-frontend、java-backend、ts-agent、deployment）/MEMORY.md。
- **TS Agent src 分包重构（2026-09-04，代码结构优化）**：按业务域拆分 `src/` 第一层平铺的 11 个文件——`app/`（`app.ts` 装配 + `config.ts` 配置）、`interview/`（`index.ts` 五维访谈 + `wireframe.ts` 线框 + `guardrails.ts` 输入护栏 + `context.ts` run.context）、`workflow/`（`index.ts` 工作流驱动 + `machine.ts` 状态机 + `events.ts` SSE 事件模型）、`llm/index.ts`（脚本化假 LLM）、`prompts/index.ts`（提示词加载器并入 `src/prompts/`，消除 `prompts.ts` 与 `prompts/` 并存）；顶层仅保留 `src/index.ts` 入口。`config.ts`/`prompts.ts` 的 agentRoot 按源码位置推断由 `..` 改为 `../..`（移入子目录后层级变深，测试直引源码的关键）。命令证据：`node scripts/run.mjs type-check` 通过；`node scripts/run.mjs test` → **85/85 passed**；`node scripts/run.mjs build` → `wsl-rt-env/ts-agent/dist/app.bundle.mjs` 2.3MB 构建成功；`git diff --check` 通过。
- **前端 dev 环境两连修（2026-09-05，#12 交付后缺陷 + spec 评审闭环）**：① `318f099` 移除 dev/preview/build-only 的 `--configLoader runner`——runner 模式下 config 加载完临时 runner 即关闭，vue/vue-devtools 插件运行期经 runner 懒加载模块报 `Vite module runner has been closed`（Windows 与 WSL 均复现；此前 #12 e2e 仅 HTTP 层验证未触达 vue transform，属验证盲区）；#12 引入 runner 规避的 EROFS 前提经实测不成立（Vite 7 bundle 模式启动零警告、`$HOME` 与项目内 `.vite-temp` 均未落盘报错，项目内 node_modules 可写）。**边界**：ts-agent vitest 的 runner 保留且健康（85/85）——崩溃仅限 dev server 插件运行期懒加载路径，勿顺手改动 vitest 的 runner。② `9fd75f1` 整页白屏修复——用户浏览器 console 实证 `does not provide an export named 'addCustomTab'/'Fragment'/'message'`：`runtimeDependencyResolver` 以 `enforce: 'pre'` 把裸导入直指包的 CJS main（vue/index.js、ant-design-vue/lib/index.js），绕过依赖预构建致浏览器原生加载 CJS；修复为普通顺序（Vite 7 中 vite:resolve 先于 normal 用户插件，预构建互操作优先命中），实测 main.ts import 全部改指 `vite-cache/deps` 预构建产物（含标准 export 块与 Fragment）。③ `1672bf0` 兜底加固（spec 评审发现兜底真触发仍返回 CJS main 会复现白屏）——`resolveRuntimeModule` 仅对裸包名导入返回 ESM 入口（嵌套 import conditions 取 default 子条目；CJS-only 包 axios/dayjs 与 deep import 返回 null，由 Vite 显式报错，明确报错优于静默白屏）；该兜底在当前包集差集=0 下不可达，属极端场景保险。命令证据：`npm run type-check` 通过、`npm run lint` 通过、dev 冒烟（main.ts import 指向 deps 预构建产物、App.vue/HomePage.vue 200）。评审记录：Standards 轴 0 违规；Spec 轴 6 项发现（文档未同步、兜底 CJS 风险、EROFS 断言无证据）已全部闭环于本次。文档同步：vue-frontend.md（runner/白屏坑修正 + vitest 边界）+ MEMORY.md 摘要。
- **#11 契约对账完成（Issue #11，2026-09-07）**：基于合流后的真实实现（TS 139/139 基线）重跑 84 例逐文件语义对账。**缺口补齐 5 项**：① golden e2e 全链 ×2（`test/golden-e2e.test.ts` + `test/fixtures/golden_{html,multi_file}.json`，对齐旧 test_e2e.py 夹具语义；假 LLM 新增 `multi-file` 剧本（`src/llm/index.ts` 一轮并行 writeFile×3）+ routes `SCRIPT_WHITELIST` 放行）；② 质检拼接补强（dist 跳过断言 + 目录缺失返回空，`review.test.ts`）；③ 十工具名驼峰绑定契约（`test/tools-contract.test.ts`，文件六件 + 图片四件，Java ToolManager 依赖）；④ 护轨拒绝 → failed 完成回调断言（`stream.test.ts` 追加）；⑤ 完成回调失败不阻断主流程（`stream.test.ts` fakeRunClient 注入 /complete 500 仍 done）。对等报告终稿 `docs/ts_agent/contract-parity.md`：**72 覆盖 / 3 语义差异（P3 定夺）/ 9 有意演进·退役（判据引架构 §退役/§3.2 或票据记录）**，T21「契约对等」判据**通过**；P3 修正项 5 条随报告移交（codeGenType 回退 vs 422、message 必填文档不一致、质检兜底方向、parsing.ts/提示词死代码、单文件原子性）。命令证据：`cd paimeng-ai-code-agent && npm test` → **144/144 passed**（19 文件，原 139 + 本票 5）+ `npm run type-check` 通过。文档同步：`.agents/memories/ts-agent.md`、`MEMORY.md`。
- **#11 code-review 双轴整改闭环（2026-09-08，#11 交付后补跑审查并按发现整改）**：Standards 轴 0 硬违规 + 4 判断性提示（最重：`frames`/`fakeRunClient`/`RunCall` 在 stream.test 与 golden-e2e 双份定义且已分叉——提取至 `test/helpers.ts` 统一签名，golden 分叉消除）；Spec 轴 4 条 AC 全部真实支撑 + 3 发现。整改：① 测试辅助提取 helpers.ts（两测试文件改引用，`npm test` 144/144 复验）；② `tools-contract.test.ts` 硬编码 `/tmp/paimeng-tools-contract` 改 `makeWorkspaceRoot()`；③ contract-parity.md §四证据订正：#10「70/70」系交付时点旧数，整改后在档证据 **77/77**（CreditServiceImpl 22 + GenerationRunServiceImpl 33 + Controller 22）——T21 门禁证据以最新数引用；④ MEMORY.md 补末尾换行符。**mock 层测试债记 #13**：`src/llm/index.ts` buildPageContent 约 100 行 MSW 风格拦截层（fetch/XHR 补丁 + fixture 规则表，自标 #13 L1 预览态）系随 2519ced 提前落地、无测试覆盖——经用户定夺保留，#13 补覆盖测试并销账（#13 评论 + 报告 §一范围注记在档）。遗留判断性提示：新增剧本需 4 处编辑（可聚映射表）、`GoldenFixture.codeGenType` 含 `vue_project` 无夹具。type-check 通过。
- **档位定价修正：快速半价（2026-09-08，用户走查反馈驱动）**：原实现 fast/standard 同系数 1（契约标注「预留」），钱包视角两档等价、选快速无经济动机。定为 **fast=0.5 / standard=1 / deep=2**（快速档半价折扣：非推理模型成本更低）。① Java（计费权威）：`AgentProperties.Credit` 三档系数 int→double（fast 默认 0.5）+ `CreditServiceImpl.calcFrozenAmount` 双精度计算并四舍五入落整数积分 + `application.yml` `fast-multiplier: 0.5` + `AgentIntensityEnum` 注释同步；新增测试 `freeze_fastTier_discountsHalfPrice`（html×1 × fast×0.5 = 50）。② TS：`src/intensity.ts` `INTENSITY_TIERS.fast.priceMultiplier = 0.5`（契约镜像，注释去「预留」、注明 Java 为计费权威）+ `test/intensity.test.ts` 三档定价断言更新。③ 前端：`IntensitySelector.vue` `TIER_MULTIPLIERS.fast = 0.5` + 预估积分 Math.round 对齐。④ 契约：`contract.md` intensity 字段注明三档系数与冻结公式。命令证据：`./mvnw test -Dtest=CreditServiceImplTest` 通过、`npm test` 全绿、前端 `type-check`/`lint` 通过（详见提交）。
- **真实 LLM provider 四档接线完成（2026-09-08，选型落地 + 深度档变更 qwen3.7-plus）**：三渠道 OpenAI 兼容 API 统一封装为与假 provider 同形的 customProvider（新模块 `src/llm/real.ts`）——① 智谱 BigModel（路由 `glm-4-flash-250414` + 快速 `glm-4.7-flash` + 质检档复用快速模型）② OpenRouter（标准 `nvidia/nemotron-3-ultra-550b-a55b:free`）③ 阿里云 DashScope Coding（深度 `qwen3.7-plus`，baseUrl `coding.dashscope.aliyuncs.com/v1`；本票用户变更：深度档弃 glm-5.3-flash 官方自备方案改走该渠道）。关键设计：`AgentRouteOptions.provider` 类型放宽为 `LlmProvider`（`Omit<ScriptedLlmProvider,'records'>`），路由层 `buildAgentRoutes` 按配置装配（任一渠道 key 配置即走真实链路，全空回退离线假 LLM）；渠道固定分工 + `scripted-*` 别名与配置 id 双注册；**智谱 thinking 关闭补丁**（`thinking:{type:'disabled'}` 注入路由/快速/质检请求——reasoning 计入 max_tokens，快速档 3000 上限会被思考吃掉）；**错误归一化**（智谱 1305 挤爆与 OpenRouter 上游 502 均以 HTTP 200 包 `{"error":...}` 返回，AI SDK 对 200 不重试且报 Invalid JSON response——渠道 fetch 层检测 error 体转写真实状态码 429/502，SDK 内建 maxRetries 对瞬时过载生效）；`buildTestApp` 强制清空渠道密钥保障 144 例测试离线（与 javaInternalToken 同模式）；config 新增 `MODEL_ROUTER` + 三渠道 key/baseUrl 键，`.env.example` 同步模板（真 key 只进 .env 不提交）。命令证据：`npm run type-check` 通过；`npm test` **144/144**；esbuild 临时冒烟（AGENT_ROOT 注入 + 真实 src 路径）：路由 586ms/快速工具环 `finish=tool-calls toolCalls=[writeFile]`（首轮 6.7s、次轮 57.8s——智谱免费池时延方差大）/标准 `text="ok"` 2.2s（归一化重试生效）/深度 22.7s。依赖新增：`@ai-sdk/openai-compatible@3.0.44`（peer zod 已有 v4.5.4；产出 v4 spec 模型经 `ai@7.0.92` customProvider 包装实测兼容）。
- **真实 LLM 接入 code-review 双轴整改闭环（2026-09-08，同日交付后即审即改）**：Standards 轴 1 硬违规（渠道部分配置语义四处矛盾：isRealLlmConfigured「任一 key」vs createRealLlm「缺一即抛」vs .env.example/config.ts 注释口径打架）+ 6 判断性提示；Spec 轴 6 条 spec 全落地，缺口 real.ts 零测试覆盖 + 归一化转码过粗。整改：① 语义统一 **fail-fast**（三渠道密钥须齐全，缺一拒启；isRealLlmConfigured/createRealLlm/config.ts/.env.example 四处口径对齐——部分配置运行时随机失败劣于启动失败）；② real.ts 渠道表驱动重构（ChannelSpec/TierRouting 收拢，三处 createOpenAICompatible 同形重复消除）+ safeJsonParse 提取 + 归一化守卫收窄到 /chat/completions 非流式且重建响应保留原头（仅删 content-encoding/length）；③ 归一化转码细化：上游 4xx/5xx 原样、1305→429、其余业务码→400 快速失败（鉴权类不空转重试）、非 JSON→502；④ LlmProvider 改 `Pick<ReturnType<typeof customProvider>,'languageModel'>`（以 SDK 工厂形态为准，消除替身寄生）；⑤ 新增 `test/llm-real.test.ts` 12 例离线单测（stub 全局 fetch 零外呼：fail-fast×4/别名双注册×2/thinking 补丁×3/归一化转码×5 组内合并）。保留判断性提示：角色名键（fast/standard 等非 scripted 非配置 id）无调用点但随渠道表顺带注册（成本 0，留作语义别名）。命令证据：`npm run type-check` 通过；`npm test` **156/156**（原 144 + 新增 12）；重构后真实链路回归冒烟：标准档 1775ms/60.6s 各返回 "ok"（智谱 1305 时段性过载经归一化正确走 429 重试后抛可读错误，curl 直连同现 1305 证实为外部因素非回归）。
