{
  "name": "paimeng-ai-code-agent",
  "version": "0.1.0",
  "private": true,
  "description": "派蒙 AI 应用工坊 TS Agent：需求访谈 / 线框 / codegen 工作流，SSE 直连浏览器",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "npm run build && npm run start",
    "build": "esbuild src/server/index.ts --bundle --platform=node --format=esm --sourcemap --banner:js=\"import { createRequire } from 'node:module'; const require = createRequire(import.meta.url);\" --outfile=dist/app.bundle.mjs",
    "start": "node --enable-source-maps dist/app.bundle.mjs",
    "type-check": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@ai-sdk/openai-compatible": "^3.0.44",
    "@ai-sdk/provider": "^4.0.10",
    "ai": "^7.0.92",
    "fastify": "^5.0.0",
    "jose": "^6.0.0",
    "pg": "^8.23.0",
    "xstate": "^5.32.6",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/pg": "^8.23.1",
    "esbuild": "^0.28.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
