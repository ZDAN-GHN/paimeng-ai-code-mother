// 重试策略单源（Issue #20 审查整改）：短调用（质检、超限收尾指令）恢复 SDK 默认退避重试的
// 次数在此一处定义。渠道层已把 429/502 归一化为可重试错误，瞬时过载可自愈；2 即 SDK 默认值，
// 显式写出便于调整。长生成主流保持 0（分钟级生成中途失败重试要整段重烧 token 与积分，
// 宁可快速失败）——仅 streamText 一处使用，就地注释不提取常量。
export const SHORT_CALL_MAX_RETRIES = 2
